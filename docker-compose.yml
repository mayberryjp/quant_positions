services:
  quant_positions:
    image: mayberry4477/quant_positions:latest
    container_name: quant_positions
    stdin_open: true
    tty: true
    environment:
      - DATABASE_URL=postgresql+psycopg://quant:quant_dev_password@postgres:5432/quant
      - API_PORT=8015
      - RECONCILIATION_INTERVAL=300
    ports:
      - "8015:8015"
